/*
 * 통합실적의 시간축 — **정본은 `shared/lib/timeAxis`** 로 옮겼다.
 *
 * 기준일은 이제 통합실적만의 조회 조건이 아니라 앱 전체가 함께 서는 축이다(조립·의장·
 * 설비도 같은 `?date=` 를 읽는다). 축을 한 곳에 두되, 이 경로를 쓰던 호출부·테스트가
 * 그대로 돌도록 얇게 재수출한다 — 옮기는 일과 고치는 일을 한 커밋에 섞지 않는다.
 */
export * from '../../../lib/timeAxis'
