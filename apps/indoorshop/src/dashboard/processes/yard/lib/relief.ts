/**
 * 3D 세우기 상수·면 색 — `shared/features/yard-map` 이 소유한다.
 * 야드 모듈 안의 기존 참조를 그대로 두기 위해 다시 내보낸다(re-export).
 */
export {
  RELIEF_METERS,
  BUILDING_EXTRUDE,
  BAY_WALL_ALPHA,
  BAY_ROOF_ALPHA,
  type ExtrudeStyle,
} from '../../../shared/features/yard-map/lib/relief'
